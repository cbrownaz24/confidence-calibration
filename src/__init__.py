"""Algorithm package.

Every module here registers one confidence-calibration algorithm via
``@register_algorithm``. ``plot_experiments.py`` imports them all automatically,
so adding an algorithm is just dropping a new file in this directory.
"""
